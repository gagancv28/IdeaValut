{
  "name": "all-in-one-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -n \"client,admin,server\" -c \"cyan,magenta,green\" \"npm run dev -w client\" \"npm run dev -w admin\" \"npm run dev -w server\"",
    "dev:client": "npm run dev -w client",
    "dev:admin": "npm run dev -w admin",
    "dev:server": "npm run dev -w server",
    "build": "concurrently \"npm run build -w client\" \"npm run build -w admin\"",
    "build:client": "npm run build -w client",
    "build:admin": "npm run build -w admin"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
